# Copie este arquivo para ".env" e troque o valor abaixo pela sua senha.
# O arquivo .env NÃO sobe para produção (está no .gitignore).
# Apenas quem tiver esta chave consegue rodar o script de atualização.
CHAVE_ACESSO=troque-esta-senha
